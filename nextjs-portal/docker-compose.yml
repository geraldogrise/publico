services:
  web:
    build: .
    container_name: nextjs-portal
    ports:
      - "3000:3000"   # http://localhost:3000
    environment:
      - NODE_ENV=production
      - NEXTAUTH_URL=http://localhost:3000
      - NEXTAUTH_SECRET=troque-em-producao-com-um-segredo-forte
      - DATABASE_URL=file:./dev.db
